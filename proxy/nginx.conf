server {
    listen 80;

    location /health {
        proxy_pass http://api:8000/;
    }

    location / {
        return 200 "Clinic platform is up\n";
        add_header Content-Type text/plain;
    }
}